from random import randint
size = 10  # Adjust this to the desired size of your array
random_array = []

for i in range(size):
    while True:
        value = random.randint(0, size - 1)
        if value != i and value not in random_array:
            random_array.append(value)
            break

print(random_array)